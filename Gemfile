source 'https://rubygems.org'

require 'json'
require 'open-uri'
versions = JSON.parse(open('https://pages.github.com/versions.json').read)

#gem 'activesupport', '4.2.7', group: :jekyll_plugins
gem 'github-pages', versions['github-pages'], group: :jekyll_plugins
